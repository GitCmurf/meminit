OUTPUT_SCHEMA_VERSION = "1.0"
# v2 is currently scoped to `check` JSON payloads. Keep other commands on v1
# until their envelopes are migrated and contract-tested.
CHECK_OUTPUT_SCHEMA_VERSION = "2.0"
