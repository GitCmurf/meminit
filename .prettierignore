# Meminit owns these bytes; Prettier must not reformat them.
#
# Rationale: these files are machine-generated or hash-locked by Meminit (the
# MEMINIT_PROTOCOL managed blocks embed a sha256 over their exact payload, and
# the index artifacts are regenerated verbatim by `meminit index`). Letting
# Prettier rewrite them changes bytes Meminit controls and breaks integrity
# checks (`meminit protocol check`) or causes regeneration churn. Formatters own
# hand-authored source; Meminit owns generated/managed artifacts — disjoint
# ownership, by design. See PLAN-016.

# --- Protocol-managed assets (hash-locked via MEMINIT_PROTOCOL markers) ---
/AGENTS.md
/.agents/skills/meminit-docops/

# --- Canonical sources for the managed/packaged assets (deployed verbatim) ---
/src/meminit/core/assets/

# --- Generated index artifacts (regenerated by `meminit index`) ---
/docs/01-indices/meminit.index.json
/docs/01-indices/catalogue.md
/docs/01-indices/kanban.md
/docs/01-indices/kanban.css

# --- Governed template trees (Meminit {{placeholder}} syntax) ---
/docs/00-governance/templates/
